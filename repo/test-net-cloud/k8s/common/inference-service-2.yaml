apiVersion: v1
kind: Service
metadata:
  name: inference-2
  labels:
    app: inference-2
spec:
  type: ClusterIP # Default, but explicit for clarity
  selector:
    app: inference-2
  ports:
  - name: poc-api
    port: 8080
    targetPort: 8080
  - name: inference-api
    port: 5000
    targetPort: 5000 